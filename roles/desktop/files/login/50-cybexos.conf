[Service]
# Read the short-lived boot passphrase through systemd's standard PAM module.
KeyringMode=inherit
# The helper handles only nonsecret policy and a boot-lifetime marker.
ExecStartPre=/usr/libexec/cybexos-login-prepare
