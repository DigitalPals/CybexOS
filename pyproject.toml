[tool.ruff]
target-version = "py311"
line-length = 100
# Model Usage's upstream tests, vendored unchanged (see scripts/sync-model-usage).
extend-exclude = ["tests/model-usage"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
