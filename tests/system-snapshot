#!/usr/bin/env bash
# Recovery points: creation and retention, the GRUB recovery submenu, restore
# with interruption recovery, and the dracut overlay hook (fixture-based).
set -euo pipefail

repo_root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
PYTHONDONTWRITEBYTECODE=1 python3 -B "$repo_root/tests/system-snapshot.py"
printf 'Btrfs recovery points, boot menu, restore, and overlay hook passed\n'
